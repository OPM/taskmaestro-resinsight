[project]
name = "taskmaestro-resinsight"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "taskmaestro==0.1.0",
    "rips",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
extend-exclude = ["vendor"]
