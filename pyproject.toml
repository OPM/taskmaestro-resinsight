[project]
name = "taskekrabbe-resinsight"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "taskekrabbe",
    "rips",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
extend-exclude = ["vendor", "taskekrabbe"]
